<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Add Skill</title>
    <link rel="stylesheet" href="/css/styles.css">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css">
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js" integrity="sha384-YvpcrYf0tY3lHB60NNkmXc5s9fDVZLESaAA55NDzOxhy9GkcIdslK1eN7N6jIeHz" crossorigin="anonymous"></script>
</head>
<body>

<div class="messages">
    <%- include('partials/messages') %>
</div>

<%- include('partials/header', { title: 'Welcome, admin!'}) %>
<h1>Añadir nuevo Skill</h1>

    <div class="container-edit">
    
    <form action="/skills/<%= skillTreeName %>/add" method="POST" enctype="multipart/form-data">
        <label for="text">Skill Text:</label>
        <textarea id="text" name="text" rows="2" required><%= skill.text %></textarea>
        <label for="description">Description:</label>
        <textarea id="description" name="description" rows="4"><%= skill.description %></textarea>

        <label for="tasks">Tasks to Complete (one per line):</label>
        <textarea id="tasks" name="tasks" rows="4" required><%= skill.tasks %></textarea>

        <label for="resources">Resources (one per line):</label>
        <textarea id="resources" name="resources" rows="4" required><%= skill.resources %></textarea>

        <label for="score">Skill Score:</label>
        <input type="number" id="score" name="score" value="<%= skill.score %>" min="1">

        <label for="icon">Upload Icon (30x30 png):</label>
        <input type="file" id="icon" name="icon" accept=".png">

        <div class="form-actions">
            <button type="submit" class="btn save">Add new Skill</button>
            <a href="/skills">
                <button type="button" class="btn cancel">Cancel</button>
            </a>
        </div>



    </form>
    </div>
</body>
</html>